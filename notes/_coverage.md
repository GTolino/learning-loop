# _coverage — advisor manifest (generated)

> Written by `generate_index.py` from the same frontmatter walk as `INDEX.md`,
> so the two cannot drift. **Never edit by hand.**
>
> This is the summary-free projection, for **coverage** questions only: does a
> note exist for concept X · what is its status · what is a goal still missing.
> For what a note SAYS, read the note. For a scannable map with summaries, read
> `INDEX.md`. Names are plain text — wrap them in `[[ ]]` when writing the plan.
>
> The review queue is not duplicated here: `status: wip` is due now, `⏳` marks a
> deferred trigger. Grep for either.

**1 notes** · 1 topics · 0 🗺 hubs

**Status:** 1 stable.

## Notes

`status | topic | filename` — plus `| title:` when the concept name differs from the filename, and `| ⏳` for a review trigger.

```
stable | example-topic | Atomic Notes — One Concept Per File
```

## Gaps — linked but never written (1)

Concepts referenced in `[[wikilinks]]` with no matching note or alias. Each
owes a disposition — write the note, or fix the link that points at it.

```
wikilinks  — 2×
```

